import { test, expect } from '@playwright/test';

test.describe('Test group', () => {
  test('seed', async ({ page }) => {
    // this spec file is needed for playwright planner agent to function. It is intentionally left blank.
  });
});
